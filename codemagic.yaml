workflows:
  flutter-build:
    name: Flutter Build
    working_directory: flutter  # 👈 set the correct subfolder
    environment:
      flutter: stable
    scripts:
      - flutter pub get
      - flutter build apk
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
